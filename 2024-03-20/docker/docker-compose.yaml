version: '3.7'
services:
    postgres:
        image: postgres:10.5
        restart: always
        environment:
          - POSTGRES_USER=root
          - POSTGRES_PASSWORD=Abacate123
        ports:
          - '5438:5432'